// Template for what human races exist. This isn't strictly a content template, as there's never
// an archetype to associate with each race, but it makes the most sense in that grouping.
class LWCERaceTemplate extends LWCEContentTemplate;

var config name Race;