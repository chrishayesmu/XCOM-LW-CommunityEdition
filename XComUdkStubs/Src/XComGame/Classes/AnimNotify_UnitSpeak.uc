class AnimNotify_UnitSpeak extends AnimNotify
    native(Animation)
    dependson(XGGameData)
    editinlinenew
    collapsecategories
    hidecategories(Object);

var() ECharacterSpeech SpeechEvent;
